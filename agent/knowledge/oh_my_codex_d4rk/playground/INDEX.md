# oh_my_codex_d4rk / playground

- **doc_drawer_oh_my**: # Autoresearch Research Showcase  This folder collects **small, reproducible research-style demos** used to showcase `om...
- **doc_drawer_oh_my**: ts - `scripts/run-autoresearch-showcase.sh` — convenience launcher for the bundled showcase missions  Naming convention:...
- **doc_drawer_oh_my**: case runner maps short names (for example `bayesopt`, `latent`, `sorting`) to mission dirs  ## Index  | Showcase | Missi...
- **doc_drawer_oh_my**: ptimization.py` | ✅ completed | ROC AUC improved from `0.9458071278825997` to `0.9976939203354298` | | Noisy high-dimens...
- **doc_drawer_oh_my**: `2.1198297352756628` to `9.411498969440865` by switching counting sort to the observed value span |  Use `scripts/run-au...
- **doc_drawer_oh_my**: 5936 | | Latent subspace discovery | 3.7019658949006504 | 4.176124116152444 | +0.47415822125179353 | | Adaptive sorting ...
- **doc_drawer_oh_my**: f-hosted code optimization loop where autoresearch removes an unnecessary shell-out from OMX itself.  ### 2. Kaggle-styl...
- **doc_drawer_oh_my**: ter search on a deterministic tabular classification benchmark with a score-improvement keep policy.  ### 3. Noisy high-...
- **doc_drawer_oh_my**: n switched from random search to a subspace-aware fixed-kernel GP with denoised incumbent selection.  ### 4. Latent subs...
- **doc_drawer_oh_my**: ul structure is mixed through latent directions rather than directly exposed as obvious coordinates.  ### 5. Adaptive so...
- **doc_drawer_oh_my**: the showcase  Example:  ```bash omx autoresearch missions/noisy-bayesopt-highdim ```  Then inspect:  ```bash RUN_ID=$(fi...
- **doc_drawer_oh_my**: tive-sort-optimization.py ```  ## Repository hygiene  These showcases are meant to stay lightweight.  Please avoid commi...
- **doc_drawer_oh_my**: {   "algorithm": "hybrid_sort",   "params": {     "insertion_threshold": 12,     "run_detection_min": 10,     "counting_...
- **doc_drawer_oh_my**: from __future__ import annotations  import json from dataclasses import dataclass from pathlib import Path from typing i...
- **doc_drawer_oh_my**: .metrics.moves += count   def load_config() -> dict:     return json.loads(CONFIG_PATH.read_text())   def insertion_sort...
- **doc_drawer_oh_my**: ops.move()             j -= 1         arr[j + 1] = key         ops.move()     return arr   def merge_sort(values: list[i...
- **doc_drawer_oh_my**: if j < len(right):         out.extend(right[j:])         ops.move(len(right) - j)     return out   def counting_sort(val...
- **doc_drawer_oh_my**: = index + offset             out.extend([value] * count)             ops.move(count)     return out   def longest_non_de...
- **doc_drawer_oh_my**: .get('run_detection_min', 10))     counting_span_limit = int(params.get('counting_span_limit', 128))      if len(values)...
- **doc_drawer_oh_my**: e_sort(values: list[int], _config: dict, ops: Ops) -> list[int]:     return merge_sort(values, ops)   def build_cases() ...
- **doc_drawer_oh_my**: es.append((f'low-cardinality-{n}', [((i * 13 + 5) % 16) for i in range(n)], 1.15))     return cases   def evaluate_algor...
- **doc_drawer_oh_my**: pend({'case': name, 'weighted_cost': weighted})     return {'total_cost': total, 'cases': per_case}   def run_config(con...
- **doc_drawer_oh_my**: {   "algorithm": "bayesian_gp",   "dimension": 24,   "budget": 48,   "final_resamples": 24,   "seed": 17,   "params": { ...
- **doc_drawer_oh_my**: from __future__ import annotations  import json from pathlib import Path from typing import Any  import numpy as np from...
- **doc_drawer_oh_my**: : np.random.Generator, n: int, dim: int = DIMENSION) -> np.ndarray:     return rng.random((n, dim))   def resolve_active...
- **doc_drawer_oh_my**: lueError(f'active_dimensions must stay within [0, {dim})')     return sorted(dict.fromkeys(active))   def sample_subspac...
- **doc_drawer_oh_my**: float)     points[:, active_dimensions] = rng.random((n, len(active_dimensions)))     return points   def fit_gp(X: np.n...
- **doc_drawer_oh_my**: alpha=1e-6,         random_state=0,         optimizer=None,     )     gp.fit(X, y)     return gp   def run_random_search...
- **doc_drawer_oh_my**: pe=float)     incumbent_index = int(np.argmax(observations))     incumbent = points[incumbent_index]      resample_rng =...
- **doc_drawer_oh_my**: t_noiseless': float(noiseless_objective(incumbent)),         'incumbent': incumbent.tolist(),     }   def run_bayesian_g...
- **doc_drawer_oh_my**: _resamples', 24))     seed = int(config.get('seed', 17))     params = dict(config.get('params', {}))      n_initial_rand...
- **doc_drawer_oh_my**: sions'))      rng = np.random.default_rng(seed)     X: list[np.ndarray] = []     y: list[float] = []      while len(X) <...
- **doc_drawer_oh_my**: dim,                 active_dimensions,                 inactive_value=inactive_value,             )             mu, std...
- **doc_drawer_oh_my**: final_candidate_rng,         final_candidate_pool_size,         dim,         active_dimensions,         inactive_value=i...
- **doc_drawer_oh_my**: 'best_mean': float(final_scores.mean()),         'best_std': float(final_scores.std(ddof=0)),         'best_noiseless': ...
- **doc_drawer_oh_my**: from __future__ import annotations  import numpy as np  DIMENSION = 24 INFORMATIVE_DIMS = (1, 5, 11, 17) NOISE_STD = 0.0...
- **doc_drawer_oh_my**: p.ndarray) -> float:     x = validate_point(x)     a = x[1]     b = x[5]     c = x[11]     d = x[17]      basin = 2.6 * ...
- **doc_drawer_oh_my**: from __future__ import annotations  import argparse import json  from optimizer import load_config, run_search   def mai...
- **doc_drawer_oh_my**: {   "algorithm": "cem_search",   "dimension": 32,   "budget": 64,   "final_resamples": 24,   "seed": 23,   "params": {  ...
- **doc_drawer_oh_my**: from __future__ import annotations  import json from pathlib import Path from typing import Any  import numpy as np from...
- **doc_drawer_oh_my**: : np.random.Generator, n: int, dim: int = DIMENSION) -> np.ndarray:     return rng.random((n, dim))   def sample_subspac...
- **doc_drawer_oh_my**: float)     points[:, active_dimensions] = rng.random((n, len(active_dimensions)))     return points   def fit_gp(X: np.n...
- **doc_drawer_oh_my**: alpha=1e-6,         random_state=0,         optimizer=None,     )     gp.fit(X, y)     return gp   def infer_active_dime...
- **doc_drawer_oh_my**: enter':         return np.full(X.shape[1], 0.5, dtype=float)     return X[int(np.argmax(y))].copy()   def run_random_sea...
- **doc_drawer_oh_my**: pe=float)     incumbent_index = int(np.argmax(observations))     incumbent = points[incumbent_index]      resample_rng =...
- **doc_drawer_oh_my**: t_noiseless': float(noiseless_objective(incumbent)),         'incumbent': incumbent.tolist(),     }   def run_cem_search...
- **doc_drawer_oh_my**: .full(dim, 0.28, dtype=float)     all_points: list[np.ndarray] = []     all_scores: list[float] = []      for offset in ...
- **doc_drawer_oh_my**: n(y) // 6):]]         mean = elite.mean(axis=0)         std = np.clip(elite.std(axis=0), 0.05, 0.25)      X = np.vstack(...
- **doc_drawer_oh_my**: t_noiseless': float(noiseless_objective(incumbent)),         'incumbent': incumbent.tolist(),     }   def run_screened_b...
- **doc_drawer_oh_my**: _resamples', 24))     seed = int(config.get('seed', 23))     params = dict(config.get('params', {}))      n_initial_rand...
- **doc_drawer_oh_my**: erved'))      rng = np.random.default_rng(seed)     X: list[np.ndarray] = []     y: list[float] = []      for _ in range...
- **doc_drawer_oh_my**: active_dimensions(X_array, y_array, top_k)     anchor = choose_anchor(X_array, y_array, anchor_mode)      while len(X) <...
- **doc_drawer_oh_my**: t(candidate_points[:, active_dimensions], return_std=True)         acquisition = mu + acq_beta * std         x_next = ca...
- **doc_drawer_oh_my**: dim,         active_dimensions,         inactive_value=inactive_value,     )[:, active_dimensions]     final_candidates ...
- **doc_drawer_oh_my**: ': float(final_scores.std(ddof=0)),         'best_noiseless': float(noiseless_objective(incumbent)),         'incumbent'...
- **doc_drawer_oh_my**: from __future__ import annotations  import numpy as np  DIMENSION = 32 NOISE_STD = 0.10  # Fixed latent mixing matrix. R...
- **doc_drawer_oh_my**: .07, 0.04, 0.08, -0.02, 0.15, 0.01, -0.03, 0.05, 0.09, -0.04, 0.02, 0.11, -0.06, 0.07, 0.03, -0.01],     [0.02, 0.06, -0...
- **doc_drawer_oh_my**: .03], ], dtype=float) PROJECTION = _PROJECTION / np.linalg.norm(_PROJECTION, axis=1, keepdims=True)   def validate_point...
- **doc_drawer_oh_my**: y) -> np.ndarray:     x = validate_point(x)     centered = x - 0.5     return PROJECTION @ centered   def noiseless_obje...
- **doc_drawer_oh_my**: 04) ** 2 + 0.35 * (z3 - 0.07) ** 2     return float(basin + ridge + wave + interaction - penalties)   def noisy_objectiv...
- **doc_drawer_oh_my**: from __future__ import annotations  import argparse import json  from optimizer import load_config, run_search   def mai...
- **doc_drawer_oh_my**: {   "model": "logistic_regression",   "params": {     "C": 1.0,     "max_iter": 5000,     "solver": "lbfgs",     "random...
- **doc_drawer_oh_my**: from __future__ import annotations  import json from pathlib import Path  from sklearn.ensemble import ExtraTreesClassif...
- **doc_drawer_oh_my**: ):     model_name = config.get("model", "decision_tree")     params = dict(config.get("params", {}))      if model_name ...
- **doc_drawer_oh_my**: from __future__ import annotations  import json  from sklearn.datasets import load_breast_cancer from sklearn.metrics im...
- **doc_drawer_oh_my**: cores = model.predict_proba(X_test)[:, 1]     else:         scores = model.decision_function(X_test)      predictions = ...